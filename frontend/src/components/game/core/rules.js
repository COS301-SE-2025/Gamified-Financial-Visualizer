// Small shared constants and helpers
const CURRENCY = 'R';

const PHASES = {
  LOBBY: 'lobby',
  LOADING: 'loading',
  PLAYING: 'playing',
  RESULTS: 'results',
};

module.exports = { CURRENCY, PHASES };
